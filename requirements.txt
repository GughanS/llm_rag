# Requirements — pinned versions added per phase
# Phase 0: no runtime dependencies yet

# Phase 1: from-scratch transformer
torch==2.3.1
transformers==4.42.4
datasets==2.20.0
safetensors==0.4.3
wandb==0.17.5
pytest==8.2.2
