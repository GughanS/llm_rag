# Requirements — minimum versions per phase
# Phase 0: no runtime dependencies yet

# Phase 1: from-scratch transformer
torch>=2.3.0
transformers>=4.42.0
datasets>=2.20.0
safetensors>=0.4.3
wandb>=0.17.5
pytest>=8.2.0

# Phase 5: Serving
fastapi>=0.111.0
uvicorn>=0.30.1
redis>=5.0.7
prometheus-client>=0.20.0
